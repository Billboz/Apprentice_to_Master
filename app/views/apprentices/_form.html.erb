<%= form_for(@apprentice) do |f| %>
  <% if @apprentice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@apprentice.errors.count, "error") %> prohibited this apprentice from being saved:</h2>

      <ul>
      <% @apprentice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>

  <div class="field">
    <%= f.label 'Master Name' %><br>
    <%= f.select :master_id, options_for_select(Master.all.collect{ |i| [i.name, i.id]})  %>

  </div>

  <div class="actions">
    <%= f.submit %>

  </div>

<%end%>